<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">GetStream Example</a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li class="<%= 'active' if current_page?(root_path) %>">
                    <a href="<%= root_path %>">Trending</a>
                </li>
                <li class="<%= 'active' if current_page?(flat_path) %>">
                    <a href="<%= flat_path %>"><i class="glyphicon glyphicon-th"></i> Flat Feed</a>
                </li>
                <li class="<%= 'active' if current_page?(aggregated_path) %>">
                    <a href="<%= aggregated_path %>"><i class="glyphicon glyphicon-th-list"></i> Aggregated Feed</a>
                </li>
                <li class="<%= 'active' if current_page?(users_path) %>">
                    <a href="<%= users_path %>"><i class="glyphicon glyphicon-user"></i> People</a>
                </li>
                <% if(user_signed_in?) %>
                <li>
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <i class="glyphicon"></i>  <%= current_user.name %><b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a href="<%= me_path %>">My Profile</a></li>
                    </ul>
                </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>